{
  "name": "car-rental-application-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd car-rental/backend && npm ci --no-audit --no-fund && npm run build",
    "start": "cd car-rental/backend && npm run start"
  }
}
